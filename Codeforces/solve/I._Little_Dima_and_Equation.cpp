#include <cmath>
#include <iostream>
#include <vector>
using ll = long long;
using lpair = std::pair<ll, ll>;
using ipair = std::pair<int, int>;
void solve()
{
	int a, b, c;
	std::cin >> a >> b >> c;
	std::vector<int> vec;
	// for (int i = 0; i < (1 << 9); i++)
	// {
	// 	for (int j = 0;j < )
	// }
}
int main()
{
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(NULL);
	// int t;
	// std::cin >> t;
	// while (t--)
	{
		solve();
	}
	return 0;
}
