#include <iostream>
#include <vector>
using ll = long long;
using lpair = std::pair<ll, ll>;
using ipair = std::pair<int, int>;
void solve() // shit problem
{
	// int n, k;
	// std::cin >> n >> k;
	// if (n == 1) {std::cout << "1\n1\n"; return;}

	// if (k == 1 || k == n) {std::cout << "-1\n"; return;}

	// int p2 = k - k % 2;
	// int p3 = k + 1 + k % 2;
	// std::cout << "3\n1 " << p2 << " " << p3 << std::endl;
}
int main()
{
	std::ios_base::sync_with_stdio(false);
	std::cin.tie(NULL);
	int t;
	std::cin >> t;
	while (t--)
	{
		solve();
	}
	return 0;
}
